@model InetMarket.Models.ProductListView

@{
    ViewData["Title"] = "Список продукции";
}

<h2>Список</h2>

<p>
    <a asp-action="Create">Добавить</a>
</p>

<form action="/Products/CategorySearch" data-ajax="true" data-ajax-mode="replace" data-ajax-update="#results" id="form0" method="get" data-ajax-method="get">
    <div class="form-inline">
        <label class="control-label">Категории: </label>
        @Html.DropDownList("categoryId", Model.Categories as SelectList,
                    htmlAttributes: new { @class = "form-control" })
        <input type="submit" value="Фильтр" class="btn btn-default" />
    </div>
</form>

<div id="results"></div>